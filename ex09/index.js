function raiseToPower(b, e) {
    return Math.pow(b, e);
}

let base = 2;
let exp = 3;
let power = raiseToPower(base, exp);
console.log(power); //8

/*Math.pow(b,e) vai fazer a exponenciação de dois dois parâmetros
para que seja feito a multiplicação de 2 ao cubo(3). */